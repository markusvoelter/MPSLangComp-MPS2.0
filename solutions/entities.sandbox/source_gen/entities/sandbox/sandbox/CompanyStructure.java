package entities.sandbox.sandbox;

/*Generated by MPS */

import javax.swing.JFrame;
import java.awt.GridLayout;
import javax.swing.JComponent;
import javax.swing.JTextField;
import javax.swing.JCheckBox;
import javax.swing.JLabel;
import javax.swing.JButton;
import java.awt.event.ActionListener;
import java.awt.event.ActionEvent;
import javax.swing.JComboBox;

public class CompanyStructure {
  public CompanyStructure() {
    JFrame frame = new JFrame();
    frame.getContentPane().setLayout(new GridLayout(0, 2));
    final JComponent widget0 = new JTextField();
    final JComponent widget1 = createComboboxFor_a1();
    final JComponent widget2 = new JCheckBox();
    final JComponent widget3 = new JTextField();
    frame.getContentPane().add(new JLabel("Name"));
    frame.getContentPane().add(widget0);
    frame.getContentPane().add(new JLabel("Role"));
    frame.getContentPane().add(widget1);
    frame.getContentPane().add(new JLabel("Freelancer"));
    frame.getContentPane().add(widget2);
    frame.getContentPane().add(new JLabel("Office"));
    frame.getContentPane().add(widget3);
    JButton okButton = new JButton("Ok");
    frame.getContentPane().add(okButton);
    okButton.addActionListener(new ActionListener() {
      public void actionPerformed(ActionEvent p0) {
        Department aDepartment = new Department();
        Employee aEmployee = new Employee();
        aEmployee.setName(retrieve_name(widget0, aEmployee));
        aEmployee.setRole(retrieve_role(widget1));
        aEmployee.setFreelancer(retrieve_freelancer(aEmployee, widget2));
        aDepartment.setDescription(retrieve_description(widget3));
        System.err.println(aDepartment.toString());
        System.err.println(aEmployee.toString());
      }

      public String retrieve_name(JComponent widget0, Employee aEmployee) {
        String newValue = ((JTextField) widget0).getText();
        if (!(aEmployee.getName() > 30)) {
          throw new RuntimeException("Validation Failed" + "Name");
        }
        return newValue;
      }

      public String retrieve_role(JComponent widget1) {
        String newValue = (String) ((JComboBox) widget1).getSelectedItem();
        return newValue;
      }

      public boolean retrieve_freelancer(Employee aEmployee, JComponent widget2) {
        boolean newValue = ((JCheckBox) widget2).isSelected();
        if (!((aEmployee.getWorksAt() != null ?
          aEmployee.getFreelancer() == true :
          aEmployee.getFreelancer() == false
        ))) {
          throw new RuntimeException("Validation Failed" + "Freelancer");
        }
        return newValue;
      }

      public String retrieve_description(JComponent widget3) {
        String newValue = ((JTextField) widget3).getText();
        return newValue;
      }
    });
    JButton cancelButton = new JButton("Cancel");
    frame.getContentPane().add(cancelButton);
    frame.setSize(400, 300);
    frame.setVisible(true);
  }

  public JComboBox createComboboxFor_a1() {
    String[] elmts = {"Boss", "TeamMember"};
    JComboBox box = new JComboBox(elmts);
    return box;
  }

  public static void main(String[] args) {
    new CompanyStructure();
  }

  static {
  }
}
