package de.voelter.mps.gv.structure;

/*Generated by MPS */

import java.util.List;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import jetbrains.mps.internal.collections.runtime.backports.LinkedList;

public enum RankKind {
  same("same", "same"),
  min("min", "min"),
  max("max", "max"),
  sink("sink", "sink"),
  source("source", "source");

  private String myName;
  private String myValue;

  RankKind(String name, String value) {
    this.myName = name;
    this.myValue = value;
  }

  public String getName() {
    return this.myName;
  }

  public String getValueAsString() {
    return this.myValue;
  }

  public String getValue() {
    return this.myValue;
  }

  public static List<RankKind> getConstants() {
    List<RankKind> list = ListSequence.fromList(new LinkedList<RankKind>());
    ListSequence.fromList(list).addElement(RankKind.same);
    ListSequence.fromList(list).addElement(RankKind.min);
    ListSequence.fromList(list).addElement(RankKind.max);
    ListSequence.fromList(list).addElement(RankKind.sink);
    ListSequence.fromList(list).addElement(RankKind.source);
    return list;
  }

  public static RankKind getDefault() {
    return RankKind.same;
  }

  public static RankKind parseValue(String value) {
    if (value == null) {
      return RankKind.getDefault();
    }
    if (value.equals(RankKind.same.getValueAsString())) {
      return RankKind.same;
    }
    if (value.equals(RankKind.min.getValueAsString())) {
      return RankKind.min;
    }
    if (value.equals(RankKind.max.getValueAsString())) {
      return RankKind.max;
    }
    if (value.equals(RankKind.sink.getValueAsString())) {
      return RankKind.sink;
    }
    if (value.equals(RankKind.source.getValueAsString())) {
      return RankKind.source;
    }
    return RankKind.getDefault();
  }
}
