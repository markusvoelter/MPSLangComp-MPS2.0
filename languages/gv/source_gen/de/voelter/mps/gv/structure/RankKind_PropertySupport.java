package de.voelter.mps.gv.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class RankKind_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<RankKind> constants = ListSequence.fromList(RankKind.getConstants()).iterator();
    while (constants.hasNext()) {
      RankKind constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }

  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<RankKind> constants = ListSequence.fromList(RankKind.getConstants()).iterator();
    while (constants.hasNext()) {
      RankKind constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }

  public String fromInternalValue(String value) {
    RankKind constant = RankKind.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
